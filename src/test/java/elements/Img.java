package elements;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class Img extends BaseElement{
    public Img(WebDriver driver, By by) {
        super(driver, by);
    }
}
